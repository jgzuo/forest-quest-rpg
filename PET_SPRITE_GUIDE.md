# 🎨 宠物精灵图使用指南

## ✅ 已完成

宠物精灵图系统已集成完毕！

**新增文件**:
- `tools/pet-sprite-generator.html` - 精灵图生成器
- `tools/create-pet-sprite.js` - 自动生成脚本
- `assets/characters/pet.png` - 占位符精灵图

**修改文件**:
- `src/scenes/BootScene.js` - 添加宠物精灵图预加载
- `src/entities/Pet.js` - 使用预加载的精灵图
- `src/scenes/GameScene.js` - 移除程序化生成代码

---

## 🐾 当前宠物外观

**占位符精灵图**: 一个简单的青色发光圆形
- 位置: `assets/characters/pet.png`
- 尺寸: 32x32 像素
- 颜色: 青色/白色渐变

---

## 🎨 使用精灵图生成器

### 方法1: 可视化生成器（推荐）

1. **打开生成器**
   ```bash
   # 在浏览器中打开
   open tools/pet-sprite-generator.html
   ```

2. **选择宠物风格**

   生成器提供6种宠物风格：

   | 风格 | 描述 | 颜色 |
   |------|------|------|
   | ✨ 发光小精灵 | 圆形发光体，带表情 | 青色/白色 |
   | 🔥 元素之灵 | 火焰形态，动态感 | 橙色/黄色 |
   | ⭐ 星空精灵 | 星形设计，闪亮 | 金色/白色 |
   | 🍃 自然之灵 | 叶片环绕，自然 | 绿色 |
   | 💠 能量球 | 魔法能量，神秘 | 紫色 |
   | 🔮 魔法阵 | 符文圆圈，奇幻 | 紫色/蓝色 |

3. **下载精灵图**
   - 点击"下载精灵图"按钮
   - 选择你喜欢的宠物风格
   - 保存为 `pet.png`

4. **替换文件**
   ```bash
   # 将下载的文件移动到正确位置
   mv ~/Downloads/pet.png assets/characters/pet.png
   ```

5. **测试游戏**
   - 刷新浏览器
   - 查看新宠物外观

---

### 方法2: 自定义精灵图

如果你想创建自己的宠物精灵图：

**规范**:
- **尺寸**: 32x32 像素
- **格式**: PNG（支持透明）
- **颜色**: 建议使用发光效果
- **中心**: (16, 16)

**设计建议**:
1. **发光效果**: 使用径向渐变创建光晕
2. **表情**: 简单的眼睛和微笑（可选）
3. **颜色**: 青色、紫色、金色等魔法色彩
4. **形状**: 圆形、星形、元素形态

**工具推荐**:
- **Aseprite** - 专业像素画编辑器
- **Photoshop** - 熟悉的图像编辑
- **GIMP** - 免费替代方案
- **Piskel** - 在线像素画编辑器

---

## 🔄 更换宠物精灵图

### 快速更换

1. 打开生成器: `tools/pet-sprite-generator.html`
2. 选择新风格
3. 下载并替换 `assets/characters/pet.png`
4. 刷新游戏

### 批量生成

如果需要生成多个宠物风格的精灵表：

1. 在生成器中点击"生成精灵表"
2. 点击"下载精灵表"
3. 保存为 `pet-spritesheet.png`
4. 修改 `BootScene.js` 使用精灵表：
   ```javascript
   this.load.spritesheet('pet', 'assets/characters/pet-spritesheet.png', {
       frameWidth: 32,
       frameHeight: 32,
       startFrame: 0,
       endFrame: 5
   });
   ```

---

## 🎯 动画支持（可选）

如果想为宠物添加动画：

### 1. 创建多帧精灵图

使用生成器的"精灵表"功能，生成包含6帧的动画：
- 帧0-1: 悬浮向上
- 帧2-3: 悬浮向下
- 帧4-5: 闪烁效果

### 2. 在 BootScene.js 中加载

```javascript
this.load.spritesheet('pet', 'assets/characters/pet-spritesheet.png', {
    frameWidth: 32,
    frameHeight: 32,
    startFrame: 0,
    endFrame: 5
});
```

### 3. 在 Pet.js 中创建动画

```javascript
constructor(scene, x, y, player) {
    super(scene, x, y, 'pet');

    // 创建浮动动画
    scene.anims.create({
        key: 'pet-float',
        frames: scene.anims.generateFrameNumbers('pet', { start: 0, end: 5 }),
        frameRate: 8,
        repeat: -1
    });

    this.anims.play('pet-float', true);
}
```

---

## 📊 精灵图性能

**当前实现**:
- ✅ 单帧静态图像
- ✅ 预加载到内存
- ✅ 性能开销极低
- ✅ 适合移动设备

**带动画的精灵图**:
- ⚠️ 6帧动画
- ⚠️ 轻微性能开销
- ⚠️ 需要更多内存
- ✅ 视觉效果更好

---

## 🐛 故障排除

### 问题 1: 宠物不显示

**原因**: 精灵图未正确加载

**解决**:
1. 检查 `assets/characters/pet.png` 是否存在
2. 打开浏览器控制台查看错误信息
3. 确认文件路径正确

### 问题 2: 精灵图变形

**原因**: 缩放比例不正确

**解决**:
在 `Pet.js` 构造函数中调整：
```javascript
this.setScale(1);  // 调整缩放比例（0.5-2.0）
```

### 问题 3: 精灵图模糊

**原因**: 渲染设置

**解决**:
在 `index.html` 中设置：
```javascript
const config = {
    type: Phaser.AUTO,
    render: {
        pixelArt: true  // 启用像素艺术渲染
    }
};
```

---

## 🎉 完成！

现在你有了一个完整的宠物精灵图系统！

**下一步**:
1. 使用生成器创建你喜欢的宠物风格
2. 替换占位符精灵图
3. 刷新游戏查看效果
4. 根据需要调整大小和位置

---

**版本**: v1.9.5
**日期**: 2026-02-04
**状态**: ✅ 精灵图系统已完成

祝你设计出独特的宠物！🐾✨
